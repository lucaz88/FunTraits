name: Mapping reads/contigs/genomes to contigs/genomes with bowtie2
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - bowtie2=2.4.5 
  - samtools=1.14
  - pip:
    - filtersam==0.0.8