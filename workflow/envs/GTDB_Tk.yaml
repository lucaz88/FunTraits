name: Taxonomic classification of genomes, MAGs and SAGs with GTDB-Tk.
channels:
  - defaults
  - bioconda
  - conda-forge
dependencies:
  - gtdbtk=2.1.0
variables:
  GTDBTK_DATA_PATH: /gpfs/data/fs71579/lucaz/_DBs/GTDBTk/release207_v2 #! has to match DB path created with 'get_DBs_config.yaml'